import styled from "styled-components";
import {bgColor} from "../App/AppStyles";

export const StyledCounterControls = styled.div`
  width: 100%;
  border-radius: 12px;
  border: 2px solid ${bgColor};
  padding: 15px;
  display: flex;
  align-items: center;
  justify-content: space-around;
`